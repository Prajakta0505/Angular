import { HtmlDirectiveDirective } from './html-directive.directive';

describe('HtmlDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HtmlDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
